# 第 10 章 避坑清单与生产实践（实战）

> 本章汇总 customer_work 源码注释里**明确记录**的框架适配坑（每条都能在其代码注释或 `docs/生产就绪评估.md` 里找到出处），以及框架自身 `SKILL.md` 记录的使用禁忌。每条注明对应的原理章节——坑的本质都是"没读懂那一章"。

## 10.1 十个真实踩过的坑

| # | 坑 | 根因与解法 | 原理 |
|---|---|---|---|
| 1 | **配了 StateStore 后模型"看不到"构建期注册的工具** | 新会话的 `AgentState.toolContext.activatedGroups` 是空列表，`ReActAgent#activateSlotForContext` 用它全量覆盖 `Toolkit#setActiveGroups`。解法：`DefaultActiveGroupsToolkit extends Toolkit` 守卫空覆盖，且 **`copy()` 必须返回自身**——否则 `ReActAgent.Builder#build` 的防御性拷贝会把子类拷回基类，守卫失效 | 第 2 章状态恢复 + 第 4 章 4.1 |
| 2 | **`HarnessAgent#streamEvents` NPE**（框架 issue #1644） | 未显式设置 `generateOptions` 时空指针。解法：`HarnessAgentFactory#defaultGenerateOptions` 永远显式传 | 第 7 章 7.5 |
| 3 | **流式接口重复吐字** | 已废弃的 `stream(...)` 按 `isLast=true` 回放整段 + 最终全文，消费侧需两级去重。解法：一律用 `streamEvents` 取 `TextBlockDeltaEvent` | 第 3 章 3.9 |
| 4 | **`block()` 落在 `reactor-http-nio` 线程抛 `IllegalStateException`** | RAG 预热/首次检索若发生在 WebFlux 事件循环线程会炸。解法：`KnowledgeProvider` 用 `@PostConstruct warmUp()` 启动期预热 | 第 9 章 9.2 |
| 5 | **沙箱 + MySQL 状态存储互斥**：Harness `SessionSandboxStateStore` 往 sessionId 拼 `/`，`MysqlAgentStateStore#validateSessionId` 拒绝 `/` | 解法：`SandboxSafeAgentStateStore` 装饰器做转义（starter 与 admin 各一份） | 第 2 章 2.6 |
| 6 | **`PermissionMode.DEFAULT` 导致 `IllegalStateException`** | `DEFAULT` 语义是"一切需显式授权规则"，没配规则时工具全进 ASKING；调用方若不实现确认回传协议（`METADATA_CONFIRM_RESULTS`）下次调用直接抛错。解法：内部系统用 `BYPASS`，对外系统实现完整确认协议 | 第 3 章 3.3/3.5 |
| 7 | **`MysqlAgentStateStore` 2 参构造硬编码库名 `agentscope`** | 解法：一律 4 参构造显式传库名（`admin.mysql.database-name`） | 第 2 章 2.6 |
| 8 | **结构化输出偶发不返回结构化数据**（框架 issue #1852/#1699） | 兜底路径把 `generate_response` 当普通工具、不用 `tool_choice` 强制，模型可能不调。解法：业务侧兜底（意图识别未命中降级 `other` + 错误指标），不要假设必然成功 | 第 3 章 3.7 |
| 9 | **OTel classpath 分裂**：`agentscope-core` 按 opentelemetry 1.61.0 编译，应用若引入旧版 api 会 api/incubator 版本分裂 | 解法：pom 里显式 `opentelemetry.version=1.61.0` 对齐 | 第 6 章 tracing |
| 10 | **RC4 → GA 升级后所有 `XxxChatModel` 类消失** | 2.0.0 GA 起模型实现从 core 拆到 `io.agentscope.extensions.model.{provider}`，五厂商都要显式声明依赖 | 第 1 章 1.4 |

另有两个值得记录的框架行为（非 bug，是没读文档就会踩的语义）：

- **`onSystemPrompt` middleware 会让 `seedSystemMsg` 同步阻塞**：`CustomerServiceService#classifyIntent` 必须 `Mono.defer + subscribeOn(boundedElastic)`，否则阻塞 WebFlux 请求线程（第 3 章 3.9）。
- **1.x 能力在 2.0 的下线清单**（`docs/MIGRATION-2.0.md`）：TTS、`AutoContextMemory`、`Pipelines` 编排、`gotoReasoning` 公开 API——迁移前先对照，别在 2.0 里找 1.x 的类。

## 10.2 框架 SKILL.md 的 Reactor 使用禁忌

agentscope-java 根目录 `SKILL.md` 的 `PROHIBITED PRACTICES` / `COMMON PITFALLS` 两节，对框架二次开发者同样适用，核心几条：

1. **响应式链里禁止 `block()`**——只允许在应用最外层边界（main、测试、非响应式框架的桥接点）；
2. **不要吞 `Mono`/`Flux` 的订阅**——组装了链却不 return/不 subscribe 等于没执行（框架内部 `fireReasoningChunk` 的 `.subscribe()` 都特意 `contextWrite` 保上下文，见第 3 章 3.4）；
3. **Reactor Context 是从下游向上游传播的**——middleware 里读 `deferContextual` 拿到的是订阅时的上下文，`AgentSpawnTool` 事件转发断链问题（第 3 章 3.5）就是没保住 Context 的后果；
4. **耗时/阻塞操作放 `boundedElastic`**——框架自身的状态持久化（`saveStateToSession`）、消息总线心跳都遵循此约定。

## 10.3 构建与运行环境（本机实测）

- **agentscope-java 构建须 JDK 17**（机器默认 JDK 8 会直接失败），本地仓库在 `~/mavenjar`；改动 core 后要先 `mvn install` core 再构建 harness。
- **customer_work 构建**：`-gs` 与 `-s` 必须同传同一 settings 文件（否则挂死在内网 Nexus）；依赖版本变更必须 `clean`；测试基线 starter 1054 + admin-server 711 + app 78 + channel 65 + gateway 1。

## 10.4 给源码阅读者的最后建议

1. **顺着 `GenerateReason` 读循环出口**（第 2 章 2.2 的表）——六个枚举值即六个出口，比从头跟 4491 行代码高效得多。
2. **改任何扩展前先想两个问题**：这个对象会被防御性拷贝吗（Toolkit）？这个状态会被 StateStore 恢复覆盖吗（activatedGroups / PermissionContext）？坑 1、6 的本质都是没想这两问。
3. **用 customer_work 当活文档**：想知道某能力的正确用法，先看它的实战代码与注释（注释里常带框架 issue 号），再回框架源码验证——比直接啃框架快一倍。
4. **examples/documentation2 是最小可运行对照**：13 个子包与官方文档章节一一对应，调试框架行为时先在那里复现。
